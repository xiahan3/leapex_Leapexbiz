# EasybookX TCSP 小程序 — V2.3 客户反馈优化

> **来源**：客户 2026-06-11 微信反馈
> **基于**：PRD_TCSP小程序_MVP.md
> **更新**：2026-06-11

---

## 一、信息架构调整（已落地小程序原型）

### 1.1 底部 Tab 调整为 4 项

| Tab | 名称 | 内容 |
|---|---|---|
| 1 | 首页 | Hero + 已绑定渠道 + 核心服务 + 进行中订单 |
| 2 | **订单服务** | 订单列表（订单 > 服务 > 账单 层级），新建订单 / 新增服务 |
| 3 | 消息通知 | 合规通知 / 流程通知 / 年审 6 轮递进 |
| 4 | 我的 | 公司 / 账单 / KYC / 设置 |

### 1.2 新用户首页 = 整页渠道入口（酷炫背景）✅

- 新用户**第一屏为全屏渠道入口**（深色 + 靛蓝/青色光晕动效 + 网格背景）
- 输入 6 位渠道码 → 校验 → 进入；或「跳过 · 自然流量」
- **已注册用户直接进入，跳过渠道码页**（localStorage `tcsp_registered` 标记）
- 首页保留「渠道码 XXX 已绑定」入口，可重新打开渠道页

---

## 二、订单 > 服务 > 账单 三级层级（已落地）

### 2.1 层级定义

```
订单 Order（套餐 / 服务申请单元）
  ├── 服务 Service A ──→ 账单 Bill A   (服务 ↔ 账单 1:1)
  ├── 服务 Service B ──→ 账单 Bill B
  └── 服务 Service C ──→ 账单 Bill C
```

| 规则 | 说明 |
|---|---|
| 订单 > 服务 | 订单层级高于服务；一个订单（如「新公司注册套餐」）含多个服务 |
| 服务 ↔ 账单 1:1 | 每个服务一一绑定一张账单（修订自 V2.2「账单↔支付 1:1」，进一步明确账单挂在服务上）|
| 订单下新增服务 | 现有订单之下可随时「新增服务」（如注册套餐里追加「商标注册」）|
| 账单生成 | 账单金额在 EasybookX 管理后台生成；具体结算线下计算 |

### 2.2 示例（小程序已呈现）

```
ORD-2026-0188 · 星辰科技 · 新公司注册套餐
  ├─ 新公司注册      → LEA-2026-0188-1  HKD 2,800  待确认（EasybookX 直接交付）
  ├─ 法定秘书(首年)  → LEA-2026-0188-2  HKD 0      已含（内嵌）
  └─ 公司印章套装    → LEA-2026-0188-3  HKD 480    待生成（供应商「金印堂」交付）
  订单合计 HKD 3,280 · [新增服务]
```

---

## 三、后台能力新增（V2.3 待开发，不在小程序客户端）

### 3.1 供应商用户角色（Supplier / Vendor）

香港 TCSP 实务中，部分交付物由第三方供应商完成（如印章制作「金印堂」、公证「公证行」、快递、法务起草）。

| 能力 | 说明 |
|---|---|
| 供应商用户 | 后台新增 `supplier` 角色，独立登录 |
| 交付物上传 | 供应商在后台为指派的服务上传交付物（如印章成品照、公证件扫描）|
| 双方账单体现 | 系统体现「EasybookX → 客户账单」与「供应商 → EasybookX 账单」两笔，**结算线下计算**，平台仅做记录与对账 |
| 数据隔离 | 供应商只能看到指派给自己的订单/服务/交付任务，看不到客户 KYC 等敏感资料 |

**数据模型增量**
```python
class Supplier:
    id, name, contact, service_types[], status, created_at

class SupplierBill:        # 供应商 → EasybookX 的应付账单
    id, supplier_id, order_id, service_id, amount, currency,
    status('待对账'|'已确认'|'已结算'), settled_offline:bool, remark

class Service:             # 订单下的服务（新增交付方字段）
    id, order_id, service_type, deliver_by('easybookx'|'supplier'),
    supplier_id?, bill_id(1:1), status
```

### 3.2 渠道佣金 + 渠道定价（灵活配置）

| 配置项 | 说明 |
|---|---|
| 渠道佣金率 | **每个渠道佣金率可不同**，后台逐渠道配置 |
| 服务级佣金率 | 同一渠道下，**不同服务可配不同佣金率**（如注册 15%、年审 8%）|
| 渠道专属定价 | **不同渠道可给用户不同价格**（渠道专属价覆盖标准价）|
| 生效时间 | 佣金率 / 渠道价支持生效起止时间 |
| 佣金结算 | 按周期生成渠道佣金报表（结算线下，平台记录）|

**数据模型增量**
```python
class ChannelCommission:
    id, channel_code, service_type, commission_rate,
    effective_from, effective_to

class ChannelPricing:
    id, channel_code, service_type, price, currency,
    effective_from, effective_to
    # 查价优先级：渠道专属价 > 客户类型差异价 > 标准价
```

**佣金计算示例**
```
客户经渠道 LEX888 下单「新公司注册」
  应收（渠道专属价）= HKD 2,800
  渠道佣金（注册服务 15%）= 2,800 × 15% = HKD 420
  → 后台生成渠道佣金记录，周期结算（线下）
```

---

## 四、本次小程序原型改动清单（已部署 /tcsp）

| # | 改动 | 状态 |
|---|---|---|
| 1 | 整页渠道入口启动页 + 酷炫动效背景 | ✅ |
| 2 | 已注册跳过渠道页（localStorage） | ✅ |
| 3 | Tab 改为 首页 / 订单服务 / 消息通知 / 我的 | ✅ |
| 4 | 订单服务 Tab：订单 > 服务 > 账单 层级 | ✅ |
| 5 | 订单详情页 + 服务↔账单 1:1 展示 | ✅ |
| 6 | 「新增服务」入口（订单下追加服务） | ✅ |
| 7 | 供应商交付服务标注（如印章「金印堂」） | ✅（客户端展示） |
| 8 | 供应商后台 / 佣金配置 | ⏳ 后台 V2.3 待开发 |

---

*文档版本：TCSP V2.3 · 2026-06-11*
